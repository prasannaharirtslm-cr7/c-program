#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    char a;
   
   
    scanf(" %c", &a);
      
    if(a=='A')
        printf("Uppercase Alphabet");
    else if (a == '5')
        printf("Digit");
    return 0;
}
